// 1 Integer Literals

/* 0
100
0xff = 255 */

// Floating Point Literals
/* 
1.9;

3.14;

3.03e23 => 3.03 * 10 to the power 23;

let billon = 1_000_000_000 */

// Arithmetic

// +, -, *, /, %, **

// let power = console.log(Math.pow(2, 9));

// let cube = console.log(Math.pow(3, 1 / 3));

// let square = console.log(Math.pow(4, 1 / 2));

// let round = console.log(Math.round(0.6));

// let ceil = console.log(Math.ceil(0.6));

// let flr = console.log(Math.floor(0.6));

// let absolute = console.log(Math.abs(-5));

// let minimum = console.log(Math.min(2, 3, 7, 4, 8, 1, -5));

// let maximum = console.log(Math.max(2, 3, 7, 4, 8));

// let rand = console.log(Math.random());
